VECTOR

-- To swap vector elements:
	V.Swap (V.First, V.Last);

-- To get element in the given index:
	V.Element (I);

-- To concatenate diffent vectors:
	V := V1 & V2 & V3;

SET

-- Basic Set Operations : Insert, Include, Replace, Delete, Exclude, Find

-- Other Set Operations : Union(and), Intersection(or), Difference(-), Symmetric difference(xor)




