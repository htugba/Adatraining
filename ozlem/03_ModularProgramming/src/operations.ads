package Operations is

   -- Only Declaration
   function incrementNumber(number : Integer;
                            incr : Integer := 0) return Integer;
   
    -- Only Declaration
   function getIncrementedValue return Integer;

end Operations;
